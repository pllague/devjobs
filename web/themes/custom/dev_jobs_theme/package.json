{
  "private": true,
  "scripts": {
    "dev": "npx tailwindcss --input src/tailwind.css --output dist/style.css --watch",
    "build": "npx tailwindcss --input src/tailwind.css --output dist/style.css --minify"
  },
  "dependencies": {
    "postcss": "^8.4.5",
    "tailwindcss": "^3.0.12"
  }
}
